#1 - the certificate that allows for access to the K8s API has specific IP addresses in it. If your K8s API IP address changes then you can't reach your cluster anymore even if its running just fine. You need to either a) get that old IP back or b) change the cert somehow.